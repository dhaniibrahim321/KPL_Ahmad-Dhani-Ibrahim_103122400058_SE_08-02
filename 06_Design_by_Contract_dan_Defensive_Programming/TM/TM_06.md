# TUGAS MANDIRI

**Nama:** Ahmad Dhani Ibrahim
**Kelas:** SE-08-02
**NIM:** 103122400058

## SOAL
Lindungi kode ini dari bilangan-bilangan "fizz buzz"!

Tugasmu adalah membuat fungsi yang menolak bilangan-bilangan kelipatan 3, 5, atau 15, menerima bilangan-bilangan bukan "fizz buzz", dan melempar yang bukan bilangan bulat.
```js
function is_not_fizzbuzz(number) {
  // TODO
}

console.log(is_not_fizzbuzz(1)) // true
console.log(is_not_fizzbuzz(3)) // false
console.log(is_not_fizzbuzz(5)) // false
console.log(is_not_fizzbuzz(30)) // false
console.log(is_not_fizzbuzz(7)) // true
console.log(is_not_fizzbuzz(null)) // Lempar TypeError
console.log(is_not_fizzbuzz(NaN)) // Lempar TypeError
console.log(is_not_fizzbuzz(Infinity)) // Lempar TypeError
```
## SOURCE CODE
[index.js](./index.js)

## PENYELESAIAN

Tambahkan JSDoc `@param {number} number` untuk memberi tahu, fungsi ini butuh satu input parameter bernama number dengan tipe data wajib integer.
Dan tambahkan `@returns {boolean}` untuk memberi tahu ketika fungsi sudah bekerja, dia akan memberikan output bertipe boolean

Lalu didalam function is_not_fizzbuzz, saya membuat logika untuk memastikan apakah data yang masuk adalah bilangan bulat, jika inputan bukan merupakan bilangan bulat, maka fungsi akan menghentikan proses running dan melemparkan typeError. 
```js
function is_not_fizzbuzz(number) {

   if(!Number.isInteger(number)){
            throw new TypeError("Error");
        }
}
```
Selanjutnya saya membuat operator modulo untuk menyaring angka, jadi jika angka adalah kelipatan 3, 5, dan 15 maka akan mengeluarkan output false. Jika angka bukan kelipatan dari 3, 5 dan 15, maka fungsi akan mengembalikan nilai true.
```js
if (number % 3 === 0) return false;
if (number % 5 === 0) return false;

return true;
```

## TERIMA KASIH!!!